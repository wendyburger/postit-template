<% header = link_to("All Post", posts_path) +  " &raquo; #{@category.name}".html_safe %>

<%= render 'shared/title_content', title: header %>

<% @category.posts.each do |post| %>
  <%= render 'posts/post', post: post %>
<% end %>